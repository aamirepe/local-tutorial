#include<bits/stdc++.h>
using namespace std;

#define ll long long

bool issquare(long long n){
    long long ans = (long long)((sqrt(n)));
    return ans*ans == n;
}

// Accumulate sum of elements in a vector
    long long accumulate(const vector<long long>& v){
    return accumulate(v.begin(), v.end(),0);
}

void solve(){
    long long n;
    cin >> n;

    
}

int main(){
    ios::sync_with_stdio(false); cin.tie(NULL); 
    long long t = 1;
    cin >> t;

    while(t--){
        solve();
    }
}
